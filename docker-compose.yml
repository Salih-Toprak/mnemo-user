services:
  container:
    build: .
    container_name: belleq-user
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - belleq-net
    restart: on-failure

networks:
  belleq-net:
    external: true
    name: belleq-net
