services:
  container:
    build: .
    container_name: mnemo-user
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - mnemo-net
    restart: on-failure

networks:
  mnemo-net:
    external: true
    name: mnemo-net
